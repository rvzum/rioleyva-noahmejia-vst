PitchUtils.h -- shared pitch helpers (piano key range, black/white key
lookup) used by Visualization. Add other cross-module helpers here as they
come up (pitch <-> note name formatting, etc.) rather than duplicating them.
